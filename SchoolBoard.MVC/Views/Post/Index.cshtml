@model SchoolBoard.Models.PostModels.PostIndexModel

<h1>@Model.Title</h1>

<div>
    Author: @Model.AuthorName
    Created: @Model.Created
</div>

<div>
    @Model.Body
</div>

@if (Model.Replies.Any())
{
    foreach (var reply in Model.Replies)
    {
        <div>
            Author: @reply.AuthorName
            Created: @reply.Created
        </div>

        <div>
            @reply.Body
        </div>
    }
}